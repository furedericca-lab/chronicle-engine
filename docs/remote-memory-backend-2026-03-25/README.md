# remote-memory-backend Docs Index

Snapshot refresh: `2026-03-25`

Current snapshot documents:

- `remote-memory-backend-contracts.md`
- `remote-memory-backend-2026-03-25-technical-documentation.md`

Snapshot clarification:

- this `2026-03-25` snapshot supersedes the `2026-03-17` remote-memory-backend snapshot where the two diverge;
- the main divergence is that command-triggered reflection generation and the remaining public reflection-named recall aliases have been removed from the supported runtime/backend contract surface;
- public backend recall naming is now behavioral-facing only;
- cadence-driven distill is now the only supported trajectory-derived knowledge-generation path;
- `session-lessons` owns lesson/cause/fix/prevention/stable-decision/durable-practice extraction;
- `governance-candidates` owns promotion-oriented governance outputs;
- `follow-up-focus` and `next-turn-guidance` are distill artifact subtypes replacing separate derived/open-loop reflection persistence;
- storage internals now use behavioral-facing names such as `category=behavioral`, `behavioral_kind`, and `behavioral:` strict-key prefixes;
- legacy reflection-era LanceDB schemas are not auto-read or auto-migrated and require manual migration or reset;
- current canonical runtime guidance lives in `../runtime-architecture.md`, `../README.md`, and `../README_CN.md`.

Current-state rule:

- use this `2026-03-25` folder as the retained remote-memory-backend snapshot for the current backend/shell boundary after the public reflection-surface removal closeout.
- the older 2026-03-17 contract state is superseded by this refreshed snapshot and should not be treated as a parallel current reference.
